{
  "name": "jaludangular",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd flowedge-app && npm install && npm run build",
    "start": "cd backend && npm install && npm start",
    "dev": "concurrently \"cd flowedge-app && npm start\" \"cd backend && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@azure/openai": "^2.0.0",
    "@vercel/functions": "^1.0.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.2",
    "multer": "^2.0.2",
    "openai": "^6.7.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}